import styled from '@emotion/styled';

export const ProjectsListStyled = styled.div`
	background: ${({theme}) => theme.color.white};
	box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.05);
	padding: 20px;
	margin-top: 16px;
`;

export const ProjectsListContentStyled = styled.div`
	
`;
